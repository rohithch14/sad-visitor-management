import { VisitorPassRequests } from './visitor-pass-requests';

describe('VisitorPassRequests', () => {
  it('should create an instance', () => {
    expect(new VisitorPassRequests()).toBeTruthy();
  });
});
